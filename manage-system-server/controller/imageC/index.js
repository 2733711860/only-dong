const { imageUpload } = require('./image');
const { imageClassify } = require('./imageType');
const  { search } = require('./search');

module.exports = {
  imageUpload,
  imageClassify,
  search
};